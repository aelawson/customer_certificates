version: '3'

services:
  api:
    build: ./
    ports:
      - 9000:9000
    volumes:
      - .:/opt/app
  api-test:
    build:
      context: ./
      dockerfile: tests/Dockerfile
    volumes:
      - .:/opt/app